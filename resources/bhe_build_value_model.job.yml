resources:
  jobs:
    bhe_build_value_model:
      name: "[${bundle.target}] BHE Value Model Build"
      max_concurrent_runs: 1
      tasks:
        - task_key: "build_value_model"
          spark_python_task:
            python_file: ../src/jobs/build_value_model.py
            parameters:
              - "--catalog"
              - "${var.catalog}"
              - "--silver-schema"
              - "bhe_silver"
              - "--gold-schema"
              - "bhe_gold"
          environment_key: "default"
      environments:
        - environment_key: "default"
          spec:
            client: "2"
      # The user running `databricks bundle deploy` is granted CAN_MANAGE
      # automatically. Add an explicit `permissions:` block here if you need
      # to share ownership with another user/group.
